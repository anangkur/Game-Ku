class Game {
  int id;
  String slug;
  String name;
  String backgroundImage;
  double rating;
  int ratingCount;

  Game(this.id, this.slug, this.name, this.backgroundImage,
      this.rating, this.ratingCount);
}
